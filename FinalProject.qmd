---
title: "Final Project"
author: "Rawan AlSarraf"
format: pdf
editor: visual
---

Testing push from R to GitHub

Testing to make sure this actually works.

```{r}
library(tidyverse)

data_raw <- read.csv("~/Desktop/IWCR/envirpeer.csv")

general_info <- data_raw %>%
  select(record_id:mm_sup_num)

wt_history <- data_raw %>%
  select(record_id, wh_ficm1:wh_approach)

my_fd_env <- data_raw %>%
  select(record_id, me_fresh_fr:mesa_foodenv_score)

social_support <- data_raw %>%
  select(record_id, me_unheathy:sallis_exercise)

ss_clean <- social_support %>%
  drop_na()


```

